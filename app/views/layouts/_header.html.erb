<nav class="navbar navbar-dark bg-dark mb-3">
  <div class="container-fluid mx-3">
    <%= link_to 'がっきょくや', root_path, class: 'navbar-brand', style: 'font-weight: bold;' %>
    <div>
      <% if current_user %>
        <button class="btn btn-light btn-sm dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <strong><%= current_user.email %></strong>
        </button>
        <ul class="dropdown-menu dropdown-menu-end me-4" aria-labelledby="navbarDropdown">
          <li><%= link_to "プロフィール", user_path(current_user.id), class: "dropdown-item" %></li>
          <li><hr class="dropdown-divider"></li>
          <li><%= button_to 'ログアウト', logout_path, method: :delete, class: 'dropdown-item' %></li>
        </ul>
      <% else %>
        <%= link_to "ログイン", login_path, class: "btn btn-light btn-sm" %>
      <% end %>
    </div>
  </div>
</nav>
